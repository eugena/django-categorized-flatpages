============
Installation
============

At the command line::

    $ easy_install django-categorized-flatpages

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-categorized-flatpages
    $ pip install django-categorized-flatpages
